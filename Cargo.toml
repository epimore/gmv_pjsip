[package]
name = "gmv_pjsip"
version = "0.1.0"
edition = "2021"

[features]
test-helpers = []

[dependencies]
base = { git = "https://github.com/epimore/pigs.git", rev = "6ef154f", features = [
    "net",
] }
# base = { package = "base", path = "../pigs/base", features = ["net"] }

encoding_rs = "0.8"
gmv_pjsip_sys = { path = "gmv_pjsip_sys" }
uuid = { version = "1.7", features = ["v4"] }
